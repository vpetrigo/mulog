if (NOT MULOG_ENABLE_DEFERRED_LOGGING)
    add_executable(realtime realtime.c)
    target_link_libraries(realtime PRIVATE mulog::mulog pthread)
else ()
    add_executable(deferred deferred.c)
    target_link_libraries(deferred PRIVATE mulog::mulog pthread)
endif ()
